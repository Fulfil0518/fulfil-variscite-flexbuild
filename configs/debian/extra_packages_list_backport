# Copyright 2025 Variscite
#
# SPDX-License-Identifier: BSD-3-Clause

# To install extra devel packages dependencies needed for compiling NXP-specific
# packages/components from source code against Debian rootfs libraries/headers
# during build-time for NXP iMX & Layerscape MPU platforms.

extra_server_devel_packages_backport=" \
    gpiod \
    libgpiod-dev \
    python3-libgpiod \
    python3-venv \
"

# Note: keep time sync via systemd-timesyncd; installing ntp pulls ntpsec and conflicts.
extra_desktop_devel_packages_backport=" \
    gpiod \
    libgpiod-dev \
    python3-libgpiod \
    python3-venv \
    apt-transport-https \
    ca-certificates \
	curl \
	gnupg \
	lsb-release \
	wpasupplicant \
	sudo \
    nano \
	htop \
	net-tools \
	ccze \
	usbutils \
	rsync \
	rsyslog
"
